# Data Directory

This directory stores the raw and processed academic data.

*   `raw/`: Raw JSON dumps from OpenAlex/Crossref.
*   `processed/`: Cleaned and vectorized data ready for analysis.

*Note: Large files are git-ignored.*
